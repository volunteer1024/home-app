# PRD v1：助眠音乐列表

## 摘要

- 产品定位：一个只提供助眠音乐的单页面应用；用户在音乐列表中直接浏览、选择和控制歌曲。
- 目标用户：家里老人和父母。界面要求路径短、按钮大、移动端优先，并兼容平板和桌面浏览器。
- 成功标准：打开应用即可看到稳定的歌曲列表；用户不离开列表页即可播放、暂停、继续播放、切换歌曲和选择播放模式。

## 当前交付范围

- 唯一页面：音乐列表（规范路径 `/sleep/list`）。应用根路径和旧链接均进入该页面。
- 歌曲浏览：展示本地静态歌单，支持按标题、别名和标签实时搜索。
- 歌曲播放：点击非当前歌曲即从头播放；歌曲始终保留在原列表位置，列表明确标识当前歌曲与播放状态。
- 暂停与继续：点击当前正在播放歌曲可暂停并保留进度；点击已暂停歌曲可从该进度继续播放。
- 切歌：点击另一首歌曲时停止原歌曲并从头播放新歌曲。
- 底部播放器：选中歌曲后显示固定底部迷你播放器，展示歌曲和播放状态，并提供播放/暂停/继续控制。
- 播放模式：列表页提供模式控件，可按 `随机`、`单曲循环`、`顺序播放` 顺序循环切换；歌曲结束时遵循当前模式。
- 异常反馈：音频无法播放时，在列表内提示错误，保留选中歌曲以便用户再次尝试。
- 本地状态：保留当前歌曲及播放进度等既有本地状态，以支持重新进入时定位到最近歌曲；不得自动播放。

## 路由与页面

- `/sleep/list`：唯一的用户页面，包含歌曲搜索、歌曲列表、当前播放状态、固定底部迷你播放器及播放模式控件。
- `/`、`/sleep`、`/sleep/player`、`/sleep/settings`、`/invoice`：兼容旧书签的入口，均跳转到 `/sleep/list`，不再渲染独立页面。
- 页面不得展示前往首页、发票、收藏、发现、我的、独立播放器或设置页的可交互入口。

## 交互规则

- 首次播放必须由用户点击触发，以满足浏览器自动播放限制。
- 当前播放歌曲显示暂停控件；暂停后显示继续播放控件。
- 搜索只过滤可见列表，不改变全局歌单顺序或当前歌曲；点击歌曲也不得改变当前列表或搜索结果的顺序和滚动位置。
- 选择歌曲、暂停、继续和切歌均在音乐列表页面完成，过程中不发生页面跳转。
- 选中歌曲后，固定底部迷你播放器显示当前歌曲和播放/暂停状态；列表为播放器与设备安全区域预留空间。
- 模式控件每次点击依次切换 `随机`、`单曲循环`、`顺序播放`，并显示当前模式。
- 播放失败时，显示易理解的错误信息；用户可直接再次点击该歌曲重试。

## 范围外

- 首页和其他家庭工具入口。
- 发票打印、收藏、发现、我的及其他未确定功能。
- 独立播放器页、设置页、离线缓存、定时停止、歌词展示、登录、后台上传和云端同步。

## 非功能要求

- 移动端优先；播放、暂停等核心控件最小点击区域为 44px。
- 支持当前主流 iOS Safari、Android Chrome、iPad Safari、桌面 Chrome、Edge 和 Safari。
- 歌曲资源使用本地静态 assets；首次进入不得自动请求或自动播放音频，直到用户触发播放。
- 播放异常不得丢失当前选中的歌曲。

## 验收场景

- 用户访问 `/` 时，直接进入音乐列表，不会看到首页或其他功能入口。
- 用户访问 `/sleep`、`/sleep/player`、`/sleep/settings` 或 `/invoice` 时，进入音乐列表，不会看到对应的独立页面。
- 用户点击一首非当前歌曲，歌曲在列表页开始播放，列表正确标识当前歌曲。
- 用户点击当前正在播放歌曲的暂停控件，歌曲暂停且进度保留；再次点击后从该进度继续播放。
- 用户在播放或暂停一首歌曲时点击另一首歌曲，原歌曲停止，新歌曲从头开始播放。
- 用户点击任意歌曲后，该歌曲和其他歌曲不置顶、不重排，当前滚动位置保持不变；底部迷你播放器更新为所选歌曲。
- 用户搜索并选择结果时，歌曲仍在列表页开始播放，搜索结果不改变顺序。
- 用户可在底部迷你播放器中暂停或继续当前歌曲，且最后一首歌不会被播放器遮挡。
- 用户连续点击模式控件时，模式按随机、单曲循环、顺序播放循环切换；歌曲结束时符合所选模式。
- 音频播放失败时，列表显示错误信息，用户仍可点击该歌曲重试。
- 在 iOS、安卓、平板和 PC 上，用户可以通过可见的 44px 以上控件完成播放、暂停和切歌。

## 假设与已知约束

- 默认技术实现为 Vite、React、TanStack Router 和本地静态资源。
- iOS 首次播放必须依赖用户手势，不支持首屏自动播放。
- 本版本只承诺音乐列表体验；未来新增页面或能力必须通过独立需求变更确定。
