## 1. 需求与测试准备

- [x] 1.1 更新 `PRD-v1.md` 的列表页交互和验收场景，明确歌曲不置顶、底部迷你播放器和三种播放模式循环切换。
- [x] 1.2 为选择歌曲后列表顺序和滚动位置不变补充失败测试，覆盖普通列表和搜索结果。
- [x] 1.3 为固定底部迷你播放器的歌曲信息、播放/暂停/继续行为及底部安全区域补充失败测试。
- [x] 1.4 为模式控件的 `随机 → 单曲循环 → 顺序播放 → 随机` 循环、可访问名称和歌曲结束行为补充失败测试。

## 2. 稳定列表与固定播放器

- [x] 2.1 移除 `SleepListPage` 的顶部特色卡片、当前歌曲过滤和所有导致点击后重排的派生列表逻辑；保持原始歌单或搜索结果的顺序。
- [x] 2.2 在原歌曲行显示当前播放或暂停状态，并确保选歌、暂停、继续和错误反馈不会改变列表滚动位置。
- [x] 2.3 实现固定底部迷你播放器，接入现有播放器状态与播放/暂停/继续操作；仅在存在当前歌曲时显示。
- [x] 2.4 更新列表与播放器样式，为固定播放器和安全区域预留空间，并在浅色与深色主题中保证状态和控件清晰可见。

## 3. 播放模式控件

- [x] 3.1 将“随机播放”快捷按钮替换为反映当前模式的循环切换控件，并接入 `playbackMode` 和 `setPlaybackMode`。
- [x] 3.2 使用现有随机队列、单曲重播和顺序下一曲逻辑，验证切换模式不会重启当前歌曲且下一曲遵循所选模式。

## 4. 验证

- [x] 4.1 执行相关单元测试、类型检查和生产构建，确认无列表重排、模式切换或样式回归。
- [x] 4.2 在移动端浏览器验证：选择任意歌曲时列表不跳动、底部播放器不遮挡最后一行、播放模式可完整循环切换。
